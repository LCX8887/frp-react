import {} from './actionTypes';

const initialState = {};

const globalsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default globalsReducer;
